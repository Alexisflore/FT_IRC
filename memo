JOIN a refaire apres les modes des canaux (invite only)  (password ?)
JOIN a adapter avec la blacklist des canaux

JOIN a adapter pour rejoindre plusieurs canaux en meme temps ? (pas preciser dans le sujet)
same pour PART

KICK a tester une fois que les clients donnent leur name
